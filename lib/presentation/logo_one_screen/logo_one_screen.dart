import 'package:flutter/material.dart';
import '../../core/app_export.dart';

class LogoOneScreen extends StatelessWidget {
  const LogoOneScreen({Key? key})
      : super(
          key: key,
        );

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: SizedBox(
          height: 218.v,
          width: 345.h,
        ),
      ),
    );
  }
}
